import React from "react";
import PropTypes from "prop-types";

const OrderListItem = ({ item }) => {
  console.log(item);
  return <div>OrderListItem</div>;
};

OrderListItem.propTypes = {};

export default OrderListItem;
